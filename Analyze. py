import pandas as pd

df = pd.read_csv("logs.csv")
print(df)

# Find attacker IP
attacker = df[df['failed_logins'] > 5]
print("\n[ALERT] Possible Brute Force:")
print(attacker)

# Top IP
print("\nTop attacker:")
print(df.groupby('ip')['failed_logins'].sum().sort_values(ascending=False))